# Custom Capabilities

The initial Edge driver intentionally uses standard SmartThings capabilities only:

- `switch`
- `switchLevel`
- `temperatureMeasurement`
- `refresh`

Add YAML capability definitions to this directory later if you want BedJet-specific mode or target-temperature controls rendered directly in SmartThings.
